// import supertest from "supertest";

// import imageRoute from "../../../routes/api/ImageRoute";
// const request = supertest(imageRoute);


// route test  for /api/images

// describe("tests for the url 'api/images'", () => {
//   it("should return a 200 status code", async () => {
//     const response = await request.get("/api/images/");
//     expect(response.status).toBe(200);
//   });
// });
//   describe("tests for the url 'api/images'", () => {
//     it("should return a 200 status code", async () => {
//       const response = await request.get("/api/images/?filename=test.jpg");
//       expect(response.status).toBe(200);
//     });

//   // route test for when a filename query is passed to the url along with height and width
//   it("should return a 200 status code", async () => {
//     const response = await request.get("/api/images/?filename=test.jpg&height=250&width=300");
//     expect(response.status).toBe(200);
//   });
//   // route test for when a filename query is passed to the url along with height and width
// });




